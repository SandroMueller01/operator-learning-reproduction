# Paper architecture: depth=4, width=40 (beta = L/N = 0.5), relu activation.
model:
  name: mlp_4x40_relu
  type: mlp
  depth: 4
  width: 40
  activation: relu

initialization:
  name: kaiming_uniform
