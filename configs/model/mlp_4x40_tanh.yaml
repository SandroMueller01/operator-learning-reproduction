# Paper architecture: depth=4, width=40 (beta = L/N = 0.5), tanh activation.
model:
  name: mlp_4x40_tanh
  type: mlp
  depth: 4
  width: 40
  activation: tanh

initialization:
  name: kaiming_uniform
